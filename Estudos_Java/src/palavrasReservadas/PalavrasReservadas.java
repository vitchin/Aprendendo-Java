package palavrasReservadas;

public class PalavrasReservadas {
	/*
	 * PALAVRAS RESERVADAS
	 * O JAVA POSSUI 52 PALAVRAS RESERVADAS
	 * ABSTRACT
	 * INT
	 * FLOAT
	 * CLASS
	 * CONST
	 * NEW - Cria uma instancia do objeto
	 * IMPORT
	 * PACKAGE
	 * SUPER 
	 * NATIVE
	 * FOR
	 * IF 
	 * ELSE IF
	 * ELSE
	 * PUBLIC
	 * PRIVATE
	 * PROTECTED
	 * CHAR
	 * DOUBLE
	 * LONG
	 * SHORT 
	 * VOID
	 * MAIN
	 * BYTE
	 * BOOLEAN
	 * EXTENDS
	 * FINAL
	 * IMPLEMENTS
	 * INTERFACE
	 * STATIC
	 * STRING
	 * STRICTFP - Precisão de dados
	 * SYNCHRONIZED - mantem a integridade de threads
	 * TRANSIENT - impede a serialização de campos 
	 * VOLATILE - indica que uma veriavel pode ser alterada durante o uso de threads
	 * break - sai do bloco de codigo em que está
	 * case
	 * throws ou throw 	
	 *
	 */
}
